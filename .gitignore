# Secrets & local state — NEVER commit
.env
.env.*
!.env.example

# Anthropic auth profile (subscription) — machine-local
.anthropic/
auth-profile/

node_modules/
dist/
*.log
.DS_Store

# Local metrics database (usage tracking)
data/

# CEG knowledge base — staff emails and decision rights. NEVER commit: this repo is public.
knowledge/
